name: Backup

on:
  schedule:
    - cron:  '*/20 * * * *'

jobs:
  backup:

    runs-on: ubuntu-latest
  
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v1
        with:
          node-version: 12

      - run: git config --global user.email bot@example.com
      - run: git config --global user.name bot
      - run: git remote set-url origin https://x-access-token:${{ secrets.TOKEN }}@github.com/2049bbs/2049bbs.github.io.git

      - run: npm install

      - run: git fetch
      - run: git checkout -f master

      - run: npm run backup
        env:
          COOKIE: ${{ secrets.COOKIE }}

      - run: git pull -X ours
      - run: git add .
      - run: git commit -m "$(date --iso-8601=minutes -u) [skip ci]"
      - run: git push
